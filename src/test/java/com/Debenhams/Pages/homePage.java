package com.Debenhams.Pages;
import org.openqa.selenium.WebDriver;

public class homePage extends basePage
{
    public homePage(WebDriver driver)// this parses the webdriver driver be used in homePage class
    {
       this.driver = driver;//the driver  can now be used in this class
    }
}
